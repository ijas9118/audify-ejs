<nav class="navbar navbar-expand-lg navbar-modern sticky-top">
  <div class="container py-2">
    <a class="navbar-brand" href="/">
      <img src="/images/svg/logo.svg" alt="Audify" height="32" />
    </a>
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav gap-lg-4 mx-auto mb-2 mb-lg-0 mobile-nav-list">
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'home') { %> active <% } %>"
            href="/"
            >Home</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'shop') { %> active <% } %>"
            href="/shop"
            >Shop</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'about') { %> active <% } %>"
            href="#"
            >About</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'contact') { %> active <% } %>"
            href="#"
            >Contact</a
          >
        </li>
      </ul>
      <div class="d-flex flex-column flex-lg-row align-items-stretch align-items-lg-center gap-2 gap-lg-3 mobile-auth-actions">
        <a href="/login" class="btn btn-secondary-modern text-center">Login</a>
        <a href="/signup" class="btn btn-primary-modern justify-content-center">Sign Up</a>
      </div>
    </div>
  </div>
</nav>
