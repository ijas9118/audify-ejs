<nav class="navbar navbar-expand-lg navbar-modern sticky-top">
  <div class="container py-2">
    <a class="navbar-brand" href="/">
      <img src="/images/svg/logo.svg" alt="Audify" height="32" />
    </a>
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav gap-lg-4 mx-auto mb-2 mb-lg-0 mobile-nav-list">
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'home') { %> active <% } %>"
            href="/"
            >Home</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'shop') { %> active <% } %>"
            href="/shop"
            >Shop</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'about') { %> active <% } %>"
            href="#"
            >About</a
          >
        </li>
        <li class="nav-item">
          <a
            class="nav-link nav-link-modern <% if (activePage === 'contact') { %> active <% } %>"
            href="#"
            >Contact</a
          >
        </li>
      </ul>

      <!-- Icons row — always horizontal -->
      <div class="d-flex align-items-center justify-content-center gap-3 mobile-icon-row">
        <a href="/shop/wishlist" class="nav-icon-link">
          <img src="/images/svg/love.svg" alt="Favorites" width="22" height="22" />
        </a>
        <a href="/shop/cart" class="nav-icon-link">
          <img src="/images/svg/cart.svg" alt="Cart" width="22" height="22" />
        </a>
        <a href="/account" class="nav-icon-link">
          <img src="/images/svg/user.svg" alt="User" width="22" height="22" />
        </a>
      </div>

      <!-- Logout -->
      <div class="d-flex flex-column flex-lg-row align-items-stretch align-items-lg-center mobile-auth-actions">
        <form action="/logout" method="POST" class="d-grid">
          <button type="submit" class="btn btn-secondary-modern text-center">Logout</button>
        </form>
      </div>
    </div>
  </div>
</nav>
